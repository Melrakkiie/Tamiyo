root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -buildvcs=false -o ./tmp/tamiyo ."
  bin = "./tmp/tamiyo"
  include_ext = ["go"]
  exclude_dir = ["tmp"]
  delay = 200
  stop_on_error = true

[log]
  time = false

[misc]
  clean_on_exit = true
